# Installation

```bash
# From crates.io (when published)
cargo install deagle-cli

# From source
git clone https://github.com/dirmacs/deagle
cd deagle
cargo install --path crates/deagle-cli
```

No external dependencies required. SQLite is bundled.
