# Installing deagle for OpenCode

## Installation

Add to the `plugin` array in your `opencode.json`:

```json
{
  "plugin": ["deagle@git+https://github.com/OWNER/deagle.git"]
}
```

Restart OpenCode. The plugin auto-installs and registers all skills.
